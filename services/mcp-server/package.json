{
  "name": "fairwins-mcp-server",
  "private": true,
  "description": "FairWins MCP server (spec 095): exposes the member API to AI agents over the Model Context Protocol. Zero dependencies, node built-ins only. It reads, it quotes, and it never signs.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js"
  },
  "version": "1.0.0"
}
