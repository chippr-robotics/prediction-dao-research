{
  "name": "fairwins-finops-exporter",
  "private": true,
  "description": "FairWins FinOps exporter (spec 089): polls every catalogued revenue and cost source and exposes them as Prometheus metrics on loopback. Read-only by construction — no signer, no write route, no public surface.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "echo 'no standalone linter wired; relies on repo-level review' && exit 0"
  },
  "dependencies": {
    "@fairwins/finops-catalogue": "*",
    "@google-cloud/bigquery": "^7.9.0",
    "ethers": "^6.17.0",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "supertest": "^7.0.0",
    "vitest": "^4.1.10"
  },
  "version": "1.11.1"
}
