// GENERATED — do not edit by hand.
//
// This file is written by `node scripts/native/sync-native-config.js` from the
// tenant manifest (`tenants/<id>/manifest.json` → `native` block), the build
// cohort, and the single version source (`scripts/release/version.js`).
// It is committed for the DEFAULT tenant so the native shells build
// reproducibly; a tenant/cohort build regenerates it, and
// `scripts/native/check-native-versions.js` fails CI on hand edits.
import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.fairwins.member',
  appName: 'FairWins',
  webDir: 'dist',
};

export default config;
