# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env

# Test coverage
coverage
.nyc_output
*.lcov
# spec 094: the E2E COVERAGE MATRIX is first-party source, not a generated report — the bare
# `coverage` rule above would silently ignore it, and a matrix nobody committed is worse than none
# (same exemption shape as scripts/coverage in the root .gitignore).
!cypress/coverage/
!cypress/coverage/**

# Lighthouse CI
.lighthouseci

# Vitest
.vitest

# Cypress
cypress/screenshots
cypress/videos
cypress/downloads
.cypress-cache
