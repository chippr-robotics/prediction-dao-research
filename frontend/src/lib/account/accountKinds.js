// Spec 086 — the display names of the account kinds, shared by every account tile. Kinds are a
// tag on one card layout, never a different card.

export const ACCOUNT_KIND_LABEL = Object.freeze({
  personal: 'Personal',
  vault: 'Multisig',
  legacy: 'Recovered',
  derived: 'Recovered',
  hardware: 'Hardware',
})
