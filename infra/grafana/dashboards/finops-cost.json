{
  "uid": "fairwins-finops-cost",
  "title": "FairWins FinOps — Cost",
  "description": "GENERATED by scripts/finops/generate-dashboards.js from @fairwins/finops-catalogue. Do not edit by hand — run `npm run finops:generate`. `npm run check:finops` fails when this file is stale.",
  "tags": [
    "finops",
    "generated",
    "cost"
  ],
  "timezone": "utc",
  "schemaVersion": 39,
  "version": 1,
  "refresh": "1m",
  "time": {
    "from": "now-30d",
    "to": "now"
  },
  "templating": {
    "list": [
      {
        "name": "datasource",
        "type": "datasource",
        "query": "prometheus",
        "current": {},
        "hide": 0,
        "label": "Data source"
      }
    ]
  },
  "panels": [
    {
      "id": 1,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "text",
      "title": "",
      "gridPos": {
        "h": 3,
        "w": 24,
        "x": 0,
        "y": 0
      },
      "options": {
        "mode": "markdown",
        "content": "# FairWins FinOps — cost sources\n\nOne panel per catalogued cost source. This page is generated from the catalogue, so a source that exists in the platform and not here is a CI failure, not an oversight."
      }
    },
    {
      "id": 2,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "table",
      "title": "Cost totals — sources currently MISSING",
      "description": "Any row here means the cost total above is PARTIAL and understated. An empty table means every live cost source was read. Sources that are merely not-configured are excluded on purpose: they are unwired features, not missing data.",
      "gridPos": {
        "h": 6,
        "w": 24,
        "x": 0,
        "y": 3
      },
      "fieldConfig": {
        "defaults": {
          "noValue": "all live sources reporting",
          "custom": {
            "align": "auto"
          }
        },
        "overrides": []
      },
      "options": {
        "showHeader": true
      },
      "targets": [
        {
          "refId": "A",
          "format": "table",
          "instant": true,
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_source_configured{kind=\"cost\",status=\"live\"} == 1 unless on(source) fairwins_finops_source_up == 1"
        }
      ]
    },
    {
      "id": 3,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "text",
      "title": "Member assistant — model provider — NOT YET LIVE",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 9
      },
      "options": {
        "mode": "markdown",
        "content": "**Not yet live.** This source is declared, and produces no value on any current deployment, so it is excluded from every total. That covers two different situations — a path that does not exist anywhere, and one that is built but switched off — and the text below says which this is.\n\nBUILT, NOT OFFERED. `POST /v1/member/assistant/chat` (spec 095) calls the Anthropic Messages API, which bills per input and output token — a real vendor cost, and the only uncatalogued one. It is enabled on no deployment (ASSISTANT_ENABLED, and MEMBER_API_ENABLED above it, are commented out), so not one token has been billed. `basis` is declared `modelled` NOW, while nothing is at stake, and it is not negotiable later: this exporter holds no Anthropic billing credential, so any dollar figure it ever publishes will be token counts times a rate typed into a config file. Only GCP is `billed` (research R1). The token counts themselves are a fact and belong in vendor usage; the dollars are our arithmetic and must always say so."
      }
    },
    {
      "id": 4,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "timeseries",
      "title": "Paymaster — sponsored gas (modelled)",
      "description": "Gas spent sponsoring member UserOps, measured as drawdown of the paymaster EntryPoint deposit. The USD figure is MODELLED: the chain charges POL, and converting needs a price. The POL drawdown itself is exact. When this deposit empties, sponsorship stops and members see failures — which is why its runway, not its balance, is what alerts.",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 9
      },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "decimals": 2,
          "noValue": "no data — source unreadable or not configured",
          "color": {
            "mode": "thresholds"
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "text",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "multi"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_cost_usd_total{source=\"paymaster-gas\"}",
          "legendFormat": "{{unit}} ({{basis}})"
        }
      ]
    },
    {
      "id": 5,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "timeseries",
      "title": "Relayer gas — Polygon (modelled)",
      "description": "Gas spent relaying intents on Polygon, measured as executor EOA balance drawdown. USD is modelled from a POL price.",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 17
      },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "decimals": 2,
          "noValue": "no data — source unreadable or not configured",
          "color": {
            "mode": "thresholds"
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "text",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "multi"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_cost_usd_total{source=\"relayer-gas-polygon\"}",
          "legendFormat": "{{unit}} ({{basis}})"
        }
      ]
    },
    {
      "id": 6,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "timeseries",
      "title": "Relayer gas — Mordor (modelled)",
      "description": "Gas spent relaying intents on Mordor, in native units. NO USD figure is produced: there is no ETC price feed, and valuing it at 0 would both understate a real cost and mean that Ethereum Classic MAINNET gas was priced at nothing too. Only the native drawdown and the runway are meaningful here — which is all that was ever needed, since running out still breaks the testnet cohort.",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 17
      },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "decimals": 2,
          "noValue": "no data — source unreadable or not configured",
          "color": {
            "mode": "thresholds"
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "text",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "multi"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_cost_usd_total{source=\"relayer-gas-mordor\"}",
          "legendFormat": "{{unit}} ({{basis}})"
        }
      ]
    },
    {
      "id": 7,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "timeseries",
      "title": "Bundler gas — Polygon (modelled)",
      "description": "Gas spent by the alto bundler executor EOA submitting bundles. USD is modelled from a POL price.",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 25
      },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "decimals": 2,
          "noValue": "no data — source unreadable or not configured",
          "color": {
            "mode": "thresholds"
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "text",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "multi"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_cost_usd_total{source=\"bundler-gas-polygon\"}",
          "legendFormat": "{{unit}} ({{basis}})"
        }
      ]
    },
    {
      "id": 8,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "timeseries",
      "title": "GCP",
      "description": "Actual charged GCP cost, per service, from the BigQuery billing export. The ONLY billed (as opposed to modelled) cost source we have. It LAGS by hours and holds nothing from before the export was enabled, so its panels state the lag rather than sitting next to a 60-second-old chain read as if equally fresh.",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 25
      },
      "fieldConfig": {
        "defaults": {
          "unit": "currencyUSD",
          "decimals": 2,
          "noValue": "no data — source unreadable or not configured",
          "color": {
            "mode": "thresholds"
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "text",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "multi"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_cost_usd_total{source=\"gcp\"}",
          "legendFormat": "{{unit}} ({{basis}})"
        }
      ]
    },
    {
      "id": 9,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "timeseries",
      "title": "Cloudflare (modelled)",
      "description": "Cloudflare spend MODELLED from the declared plan subscription. Research R1: the GraphQL Analytics API returns requests and bytes but no dollar figure, and there is no public billing API for the plan. The usage it DOES return is exported separately as vendor usage, which is a fact; this dollar figure is our arithmetic.",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 33
      },
      "fieldConfig": {
        "defaults": {
          "unit": "currencyUSD",
          "decimals": 2,
          "noValue": "no data — source unreadable or not configured",
          "color": {
            "mode": "thresholds"
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "text",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "multi"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_cost_usd_total{source=\"cloudflare\"}",
          "legendFormat": "{{unit}} ({{basis}})"
        }
      ]
    },
    {
      "id": 10,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "timeseries",
      "title": "QuickNode (modelled)",
      "description": "QuickNode spend MODELLED from API-credit consumption times the declared plan rate. Research R1: the Admin API reports credits, not dollars, and a Flat Rate RPS plan decouples spend from usage entirely — under that plan the modelled figure is the flat subscription and credit usage is informational only.",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 33
      },
      "fieldConfig": {
        "defaults": {
          "unit": "currencyUSD",
          "decimals": 2,
          "noValue": "no data — source unreadable or not configured",
          "color": {
            "mode": "thresholds"
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "text",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "multi"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_cost_usd_total{source=\"quicknode\"}",
          "legendFormat": "{{unit}} ({{basis}})"
        }
      ]
    },
    {
      "id": 11,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "timeseries",
      "title": "Grafana Cloud (this system) (modelled)",
      "description": "What the FinOps system itself costs: the Grafana Cloud plan plus the BigQuery query spend this exporter incurs reading the billing export. FR-029 — a FinOps system that hides its own cost is not credible. The free tier really is $0, but assert that by setting FINOPS_GRAFANA_PLAN_USD=0: unset reports not-configured, because \"we are on the free tier\" and \"nobody set this\" are different facts.",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 41
      },
      "fieldConfig": {
        "defaults": {
          "unit": "currencyUSD",
          "decimals": 2,
          "noValue": "no data — source unreadable or not configured",
          "color": {
            "mode": "thresholds"
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "text",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "multi"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_cost_usd_total{source=\"grafana-cloud\"}",
          "legendFormat": "{{unit}} ({{basis}})"
        }
      ]
    }
  ]
}
