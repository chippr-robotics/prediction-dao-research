{
  "uid": "fairwins-finops-usage",
  "title": "FairWins FinOps — Usage",
  "description": "GENERATED by scripts/finops/generate-dashboards.js from @fairwins/finops-catalogue. Do not edit by hand — run `npm run finops:generate`. `npm run check:finops` fails when this file is stale.",
  "tags": [
    "finops",
    "generated",
    "usage"
  ],
  "timezone": "utc",
  "schemaVersion": 39,
  "version": 1,
  "refresh": "1m",
  "time": {
    "from": "now-30d",
    "to": "now"
  },
  "templating": {
    "list": [
      {
        "name": "datasource",
        "type": "datasource",
        "query": "prometheus",
        "current": {},
        "hide": 0,
        "label": "Data source"
      }
    ]
  },
  "panels": [
    {
      "id": 1,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "text",
      "title": "",
      "gridPos": {
        "h": 3,
        "w": 24,
        "x": 0,
        "y": 0
      },
      "options": {
        "mode": "markdown",
        "content": "# FairWins FinOps — usage sources\n\nOne panel per catalogued usage source. This page is generated from the catalogue, so a source that exists in the platform and not here is a CI failure, not an oversight."
      }
    },
    {
      "id": 2,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "table",
      "title": "Usage totals — sources currently MISSING",
      "description": "Any row here means the usage total above is PARTIAL and understated. An empty table means every live usage source was read. Sources that are merely not-configured are excluded on purpose: they are unwired features, not missing data.",
      "gridPos": {
        "h": 6,
        "w": 24,
        "x": 0,
        "y": 3
      },
      "fieldConfig": {
        "defaults": {
          "noValue": "all live sources reporting",
          "custom": {
            "align": "auto"
          }
        },
        "overrides": []
      },
      "options": {
        "showHeader": true
      },
      "targets": [
        {
          "refId": "A",
          "format": "table",
          "instant": true,
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_source_configured{kind=\"usage\",status=\"live\"} == 1 unless on(source) fairwins_finops_source_up == 1"
        }
      ]
    },
    {
      "id": 3,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "table",
      "title": "Source health — usage sources — read / unreadable / not-configured",
      "description": "configured=1 & up=1 → read. configured=1 & up=0 → UNREADABLE (a real problem). configured=0 → not-configured (a feature that has not been wired up; not an outage). This is the panel that tells you which of those a blank panel above is: they look identical everywhere else, and they are not the same thing to act on.",
      "gridPos": {
        "h": 8,
        "w": 24,
        "x": 0,
        "y": 9
      },
      "fieldConfig": {
        "defaults": {
          "custom": {
            "align": "auto"
          }
        },
        "overrides": []
      },
      "options": {
        "showHeader": true
      },
      "targets": [
        {
          "refId": "A",
          "format": "table",
          "instant": true,
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_source_configured{kind=\"usage\"}"
        },
        {
          "refId": "B",
          "format": "table",
          "instant": true,
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_source_up{kind=\"usage\"}"
        },
        {
          "refId": "C",
          "format": "table",
          "instant": true,
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "time() - (fairwins_finops_source_last_success_timestamp_seconds and on(source) fairwins_finops_source_configured{kind=\"usage\"})"
        }
      ]
    },
    {
      "id": 4,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "timeseries",
      "title": "Gateway upstream usage",
      "description": "MEASURED request counts from the relay-gateway, per assurance tier and per upstream (spec 106, #1447): who is consuming the platform-held vendor credentials, in whose name. It is context for the vendor cost sources beside it — the QuickNode account is capped at 50 req/s shared with the gasless bundler, so this is the series that says WHERE that budget went. It is NOT a dollar and never joins a money total; counters are cumulative-since-boot on the gateway (rate() over them survives restarts the way any Prometheus counter does), and the labels are bounded by construction: tiers come from the fixed ladder, upstreams from the route table.",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 17
      },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "decimals": 2,
          "noValue": "no value published — see “Source health” for whether this is unreadable or not-configured",
          "color": {
            "mode": "thresholds"
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "text",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "multi"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "sum by (tier) (rate(fairwins_finops_vendor_usage{source=\"gateway-upstream-usage\",metric=\"tier_requests\"}[10m]))",
          "legendFormat": "tier {{tier}}"
        },
        {
          "refId": "B",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "sum by (upstream) (rate(fairwins_finops_vendor_usage{source=\"gateway-upstream-usage\",metric=\"upstream_calls\"}[10m]))",
          "legendFormat": "upstream {{upstream}}"
        }
      ]
    }
  ]
}
