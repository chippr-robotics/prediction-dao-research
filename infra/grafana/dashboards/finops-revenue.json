{
  "uid": "fairwins-finops-revenue",
  "title": "FairWins FinOps — Revenue",
  "description": "GENERATED by scripts/finops/generate-dashboards.js from @fairwins/finops-catalogue. Do not edit by hand — run `npm run finops:generate`. `npm run check:finops` fails when this file is stale.",
  "tags": [
    "finops",
    "generated",
    "revenue"
  ],
  "timezone": "utc",
  "schemaVersion": 39,
  "version": 1,
  "refresh": "1m",
  "time": {
    "from": "now-30d",
    "to": "now"
  },
  "templating": {
    "list": [
      {
        "name": "datasource",
        "type": "datasource",
        "query": "prometheus",
        "current": {},
        "hide": 0,
        "label": "Data source"
      }
    ]
  },
  "panels": [
    {
      "id": 1,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "text",
      "title": "",
      "gridPos": {
        "h": 3,
        "w": 24,
        "x": 0,
        "y": 0
      },
      "options": {
        "mode": "markdown",
        "content": "# FairWins FinOps — revenue sources\n\nOne panel per catalogued revenue source. This page is generated from the catalogue, so a source that exists in the platform and not here is a CI failure, not an oversight."
      }
    },
    {
      "id": 2,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "table",
      "title": "Revenue totals — sources currently MISSING",
      "description": "Any row here means the revenue total above is PARTIAL and understated. An empty table means every live revenue source was read. Sources that are merely not-configured are excluded on purpose: they are unwired features, not missing data.",
      "gridPos": {
        "h": 6,
        "w": 24,
        "x": 0,
        "y": 3
      },
      "fieldConfig": {
        "defaults": {
          "noValue": "all live sources reporting",
          "custom": {
            "align": "auto"
          }
        },
        "overrides": []
      },
      "options": {
        "showHeader": true
      },
      "targets": [
        {
          "refId": "A",
          "format": "table",
          "instant": true,
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_source_configured{kind=\"revenue\",status=\"live\"} == 1 unless on(source) fairwins_finops_source_up == 1"
        }
      ]
    },
    {
      "id": 3,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "timeseries",
      "title": "Earn — lending fee",
      "description": "Gross platform fee actually charged for `earn.lend`, summed from FeeRouter FeeCharged events over finalized blocks. It is what the treasury RECEIVED, not what was quoted and not what was billed to a member elsewhere. Several of these services ship at a rate of 0 bps, so a genuine 0 here is expected and is NOT the same as an unread source.",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 9
      },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "decimals": 2,
          "noValue": "no data — source unreadable or not configured",
          "color": {
            "mode": "thresholds"
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "text",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "multi"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_revenue_total{source=\"fee-earn-lend\"}",
          "legendFormat": "{{unit}}"
        }
      ]
    },
    {
      "id": 4,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "timeseries",
      "title": "Predict — Polymarket taker (builder fee)",
      "description": "Gross platform fee actually charged for `polymarket.taker`, summed from FeeRouter FeeCharged events over finalized blocks. It is what the treasury RECEIVED, not what was quoted and not what was billed to a member elsewhere. Several of these services ship at a rate of 0 bps, so a genuine 0 here is expected and is NOT the same as an unread source.",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 9
      },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "decimals": 2,
          "noValue": "no data — source unreadable or not configured",
          "color": {
            "mode": "thresholds"
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "text",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "multi"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_revenue_total{source=\"fee-polymarket-taker\"}",
          "legendFormat": "{{unit}}"
        }
      ]
    },
    {
      "id": 5,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "timeseries",
      "title": "Predict — Polymarket maker (builder fee)",
      "description": "Gross platform fee actually charged for `polymarket.maker`, summed from FeeRouter FeeCharged events over finalized blocks. It is what the treasury RECEIVED, not what was quoted and not what was billed to a member elsewhere. Several of these services ship at a rate of 0 bps, so a genuine 0 here is expected and is NOT the same as an unread source.",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 17
      },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "decimals": 2,
          "noValue": "no data — source unreadable or not configured",
          "color": {
            "mode": "thresholds"
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "text",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "multi"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_revenue_total{source=\"fee-polymarket-maker\"}",
          "legendFormat": "{{unit}}"
        }
      ]
    },
    {
      "id": 6,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "timeseries",
      "title": "Stake — Lido liquid staking",
      "description": "Gross platform fee actually charged for `stake.lido`, summed from FeeRouter FeeCharged events over finalized blocks. It is what the treasury RECEIVED, not what was quoted and not what was billed to a member elsewhere. Several of these services ship at a rate of 0 bps, so a genuine 0 here is expected and is NOT the same as an unread source.",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 17
      },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "decimals": 2,
          "noValue": "no data — source unreadable or not configured",
          "color": {
            "mode": "thresholds"
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "text",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "multi"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_revenue_total{source=\"fee-stake-lido\"}",
          "legendFormat": "{{unit}}"
        }
      ]
    },
    {
      "id": 7,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "timeseries",
      "title": "Stake — Polygon liquid staking",
      "description": "Gross platform fee actually charged for `stake.polygon`, summed from FeeRouter FeeCharged events over finalized blocks. It is what the treasury RECEIVED, not what was quoted and not what was billed to a member elsewhere. Several of these services ship at a rate of 0 bps, so a genuine 0 here is expected and is NOT the same as an unread source.",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 25
      },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "decimals": 2,
          "noValue": "no data — source unreadable or not configured",
          "color": {
            "mode": "thresholds"
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "text",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "multi"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_revenue_total{source=\"fee-stake-polygon\"}",
          "legendFormat": "{{unit}}"
        }
      ]
    },
    {
      "id": 8,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "timeseries",
      "title": "Bridge — transfer fee",
      "description": "Gross platform fee actually charged for `bridge.transfer`, summed from FeeRouter FeeCharged events over finalized blocks. It is what the treasury RECEIVED, not what was quoted and not what was billed to a member elsewhere. Several of these services ship at a rate of 0 bps, so a genuine 0 here is expected and is NOT the same as an unread source.",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 25
      },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "decimals": 2,
          "noValue": "no data — source unreadable or not configured",
          "color": {
            "mode": "thresholds"
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "text",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "multi"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_revenue_total{source=\"fee-bridge-transfer\"}",
          "legendFormat": "{{unit}}"
        }
      ]
    },
    {
      "id": 9,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "timeseries",
      "title": "Supply — liquidity deposit fee",
      "description": "Gross platform fee actually charged for `liquidity.deposit`, summed from FeeRouter FeeCharged events over finalized blocks. It is what the treasury RECEIVED, not what was quoted and not what was billed to a member elsewhere. Several of these services ship at a rate of 0 bps, so a genuine 0 here is expected and is NOT the same as an unread source.",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 33
      },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "decimals": 2,
          "noValue": "no data — source unreadable or not configured",
          "color": {
            "mode": "thresholds"
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "text",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "multi"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_revenue_total{source=\"fee-liquidity-deposit\"}",
          "legendFormat": "{{unit}}"
        }
      ]
    },
    {
      "id": 10,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "timeseries",
      "title": "Perps — Hyperliquid builder fee",
      "description": "Gross platform fee actually charged for `perps.hyperliquid.builder`, summed from FeeRouter FeeCharged events over finalized blocks. It is what the treasury RECEIVED, not what was quoted and not what was billed to a member elsewhere. Several of these services ship at a rate of 0 bps, so a genuine 0 here is expected and is NOT the same as an unread source.",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 33
      },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "decimals": 2,
          "noValue": "no data — source unreadable or not configured",
          "color": {
            "mode": "thresholds"
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "text",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "multi"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_revenue_total{source=\"fee-perps-hl-builder\"}",
          "legendFormat": "{{unit}}"
        }
      ]
    },
    {
      "id": 11,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "timeseries",
      "title": "Fees waived (no treasury configured)",
      "description": "Fee amounts skipped because the FeeRouter had no treasury set (FeeSkippedNoTreasury). This is revenue forgone for a fixable configuration reason. It is NOT earned revenue and is never added to any revenue total — it is displayed so that a silently misconfigured fee path stops looking like a quiet day.",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 41
      },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "decimals": 2,
          "noValue": "no data — source unreadable or not configured",
          "color": {
            "mode": "thresholds"
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "text",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "multi"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_revenue_waived_total{source=\"fee-waived-no-treasury\"}",
          "legendFormat": "{{unit}}"
        }
      ]
    },
    {
      "id": 12,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "timeseries",
      "title": "Membership — received by treasury",
      "description": "Membership fees WITHDRAWN to the treasury (FeesWithdrawn). This is money that has arrived. It is deliberately not the same number as membership-accrued, and the two are never summed (spec 071).",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 41
      },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "decimals": 2,
          "noValue": "no data — source unreadable or not configured",
          "color": {
            "mode": "thresholds"
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "text",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "multi"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_revenue_total{source=\"membership-received\"}",
          "legendFormat": "{{unit}}"
        }
      ]
    },
    {
      "id": 13,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "timeseries",
      "title": "Membership — accrued, not yet withdrawn",
      "description": "Membership fees earned (MembershipPurchased/Upgraded/Extended) and still sitting in the contract. Earned but NOT received. Adding this to membership-received would double-count every fee that has since been withdrawn, which is exactly why they are separate metric families.",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 49
      },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "decimals": 2,
          "noValue": "no data — source unreadable or not configured",
          "color": {
            "mode": "thresholds"
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "text",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "multi"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_revenue_accrued{source=\"membership-accrued\"}",
          "legendFormat": "{{unit}}"
        }
      ]
    },
    {
      "id": 14,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "timeseries",
      "title": "Collect — OpenSea referral",
      "description": "Referral share attributable to FairWins-routed OpenSea volume. OPENSEA_REFERRAL_ADDRESS is unset in production, so this reports not-configured rather than zero: no code is registered, so there is nothing to earn.",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 49
      },
      "fieldConfig": {
        "defaults": {
          "unit": "currencyUSD",
          "decimals": 2,
          "noValue": "no data — source unreadable or not configured",
          "color": {
            "mode": "thresholds"
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "text",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "multi"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_revenue_total{source=\"referral-opensea\"}",
          "legendFormat": "{{unit}}"
        }
      ]
    },
    {
      "id": 15,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "timeseries",
      "title": "Perps — Gains referral",
      "description": "Venue-paid referral share from Gains Network. Gains has not whitelisted a referrer, so link-outs go unattributed and this reports not-configured.",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 57
      },
      "fieldConfig": {
        "defaults": {
          "unit": "currencyUSD",
          "decimals": 2,
          "noValue": "no data — source unreadable or not configured",
          "color": {
            "mode": "thresholds"
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "text",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "multi"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_revenue_total{source=\"referral-gains\"}",
          "legendFormat": "{{unit}}"
        }
      ]
    },
    {
      "id": 16,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "timeseries",
      "title": "Perps — GMX referral",
      "description": "Venue-paid GMX referral share (which also discounts the trader). No ref code is registered, so this reports not-configured.",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 57
      },
      "fieldConfig": {
        "defaults": {
          "unit": "currencyUSD",
          "decimals": 2,
          "noValue": "no data — source unreadable or not configured",
          "color": {
            "mode": "thresholds"
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "text",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "multi"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_revenue_total{source=\"referral-gmx\"}",
          "legendFormat": "{{unit}}"
        }
      ]
    },
    {
      "id": 17,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "timeseries",
      "title": "Predict — Polymarket weekly builder rewards",
      "description": "Weekly rewards from the Polymarket builder-code programme, separate from the per-order builder fee captured by fee-polymarket-taker/maker. The builder code IS registered, so unlike the other referral sources this one is expected to report a real value once the credential is provisioned.",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 65
      },
      "fieldConfig": {
        "defaults": {
          "unit": "currencyUSD",
          "decimals": 2,
          "noValue": "no data — source unreadable or not configured",
          "color": {
            "mode": "thresholds"
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "text",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "multi"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_revenue_total{source=\"referral-polymarket-rewards\"}",
          "legendFormat": "{{unit}}"
        }
      ]
    },
    {
      "id": 18,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "text",
      "title": "Assistant — GutterToken referral credit — NOT YET LIVE",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 65
      },
      "options": {
        "mode": "markdown",
        "content": "**Not yet live.** This source is declared, and produces no value on any current deployment, so it is excluded from every total. That covers two different situations — a path that does not exist anywhere, and one that is built but switched off — and the text below says which this is.\n\nPLANNED, NOT LIVE — and NOT CASH. When a member opens and funds a GutterToken account through the referral-coded signup link on the Assistant tab (spec 104), GutterToken credits FairWins’ own GutterToken account with prepaid usage credit (docs: 15% of the referee’s FIRST deposit, capped at $50; non-refundable, non-cashable, self-referral void). It is in-kind credit that can only be spent on model calls from that account, so it is never revenue in USD and must never be summed with the cash sources. No referral code is registered yet and, more fundamentally, nothing can read the figure: GutterToken publishes no balance or referral endpoint, so no collector could ever report it — the honest state is not-yet-live with no metric, and it is excluded from every total. If P3 (a FairWins-owned GutterToken account as the FairWins rail’s upstream) is adopted, this credit accrues on the same account that assistant-model-api would then be billed to, and offsets that cost; the two entries should be promoted together, with this one staying an in-kind offset rather than becoming a revenue line."
      }
    },
    {
      "id": 19,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "text",
      "title": "Mini-app licenses — NOT YET LIVE",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 73
      },
      "options": {
        "mode": "markdown",
        "content": "**Not yet live.** This source is declared, and produces no value on any current deployment, so it is excluded from every total. That covers two different situations — a path that does not exist anywhere, and one that is built but switched off — and the text below says which this is.\n\nPLANNED, NOT LIVE. contracts/apps/MiniAppRegistry.sol has no fee, price or payable function as of spec 089, so there is no licensing revenue to read. Shown as not-yet-live and excluded from every total."
      }
    },
    {
      "id": 20,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "text",
      "title": "Wager platform fee — NOT YET LIVE",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 73
      },
      "options": {
        "mode": "markdown",
        "content": "**Not yet live.** This source is declared, and produces no value on any current deployment, so it is excluded from every total. That covers two different situations — a path that does not exist anywhere, and one that is built but switched off — and the text below says which this is.\n\nPLANNED, NOT LIVE. WagerRegistry escrows stakes and takes no platform cut, so wagers currently generate volume but no direct revenue. Shown as not-yet-live and excluded from every total."
      }
    },
    {
      "id": 21,
      "datasource": {
        "type": "prometheus",
        "uid": "${datasource}"
      },
      "type": "timeseries",
      "title": "Member API — x402 agent payments",
      "description": "An agent holding no membership pays per request: it signs an EIP-3009 USDC authorization to the platform treasury (X402_PAY_TO) and the gateway settles it — $0.01 a read, $0.05 a typed-data build, $0.10 an assistant message. Unambiguously revenue, and it routes around the FeeRouter entirely, which is why the coverage gate could not see it until C2 learned to look for configured platform payees. The treasury is the admin Safe, which the FeeRouter also pays into on most chains — so counting arrivals at that address would attribute fee revenue to agents. The collector counts only authorization-settled transfers, which nothing else in this estate produces at this address. Counters are cumulative SINCE EXPORTER START, not all-time: cursors are in memory by design (owning a datastore whose corruption silently understates revenue is the worse failure), and the dashboard states that rather than implying a lifetime total.",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 81
      },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "decimals": 2,
          "noValue": "no data — source unreadable or not configured",
          "color": {
            "mode": "thresholds"
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "text",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "multi"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "prometheus",
            "uid": "${datasource}"
          },
          "expr": "fairwins_finops_revenue_total{source=\"x402-agent-payments\"}",
          "legendFormat": "{{unit}}"
        }
      ]
    }
  ]
}
