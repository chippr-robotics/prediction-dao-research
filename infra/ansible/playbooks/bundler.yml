# Converge the bundler node only (spec 087).
---
- name: Converge the bundler
  hosts: bundler
  gather_facts: true
  roles:
    - common
    - docker
    - nginx
    - fairwins_secrets
    - fairwins_stack
